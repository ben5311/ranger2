Entity Kunde {
    age: random(18..60)
    gender: random("male", "female")
    firstname: map(gender => {
        "male": random("Max", "Jonas", "Sebastian")
        "female": random("Anna", "Lea", "Sofia")
        })
    vorname: map(age => {"val0":1,"val1":2,"val2":3})
    lastname: random("Ackermann", "Dietz", "Fischer")
    account: BankAccount
}

Entity BankAccount {
    balance: random(1000..15000)
    currency: random("EUR", "USD", "CHF")
}
